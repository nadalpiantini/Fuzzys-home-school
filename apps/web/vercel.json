{
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "crons": [{ "path": "/api/cron/health", "schedule": "*/5 * * * *" }],
  "functions": {
    "app/api/brain/generate/route.ts": { "maxDuration": 60 }
  }
}
