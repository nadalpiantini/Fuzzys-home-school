{
  "name": "fuzzys-home-school",
  "version": "1.0.0",
  "private": true,
  "description": "Educational platform with AI tutoring, gamified learning, and AR/QR Colonial Zone exploration",
  "workspaces": [
    "apps/*",
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "db:migrate": "cd db && npm run migrate",
    "db:seed": "cd db && npm run seed",
    "clean": "turbo run clean && rm -rf node_modules",
    "deploy": "npm run build && npx wrangler pages deploy apps/web/.next --project-name=fuzzys-home-school",
    "deploy:preview": "npm run build && npx wrangler pages deploy apps/web/.next --project-name=fuzzys-home-school --branch=preview",
    "cf:login": "npx wrangler login",
    "cf:logout": "npx wrangler logout",
    "cf:whoami": "npx wrangler whoami"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "dotenv": "^17.2.2",
    "eslint": "^8.54.0",
    "eslint-config-prettier": "^9.0.0",
    "prettier": "^3.1.0",
    "turbo": "^1.11.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "author": "Fuzzy's Home School Team",
  "license": "MIT"
}
